function solution(numbers) {
    
    

    var sum = 0

    for(var i=0;i<10;i++){

        var res = numbers.find(e => e == i)

        if(res == undefined){
            sum+=i
        }

    }
    
    var answer = sum;
    return answer;
}
