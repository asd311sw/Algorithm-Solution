function solution(s) {
    
    
    
    var answer = '';

    var len = s.length

    if(len % 2 == 0){

        var idx = len / 2
        answer = `${s[idx - 1]}${s[idx]}`
    }
    else{

        var idx = parseInt(len / 2)
        answer = s[idx]
    }

    return answer;
}
